// Keeping script empty since we moved interactions entirely to CSS hover states
document.addEventListener("DOMContentLoaded", () => {
    // Optional: Add logic here if you want to click items to persist the popup instead
});
